# JSON schemas

These schema files describe data shapes (events, weekly retros, report output, SnR import) for reference and external tooling. They are **not** loaded or validated at runtime by QI; runtime validation uses Pydantic models in the codebase.
